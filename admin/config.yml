backend:
  name: github
  repo: magallen100-ux/tienda-hogar # Cambia por tu repo real (ej: magallen100-ux/tienda-hogar)
  branch: main
  base_url: https://cortinajesivanna.com
  auth_endpoint: /api/auth

media_folder: "img"
public_folder: "img"

collections:
  - name: "categorias_galeria"
    label: "Categorías de la Galería"
    label_singular: "Categoría"
    folder: "content/galeria"
    create: true
    slug: "{{fields.titulo}}" # 👈 Forzamos a usar el título (ej: "Estores Screen" -> "estores-screen.json")
    format: "json"
    fields:
      - { label: "Nombre de la Categoría", name: "titulo", widget: "string", hint: "Ej: Estores Screen" }
      - { label: "Descripción Corta", name: "descripcion", widget: "text", required: false }
      - { label: "Orden de aparición", name: "orden", widget: "number", default: 1, value_type: "int" }
      - label: "Fotos de esta categoría"
        name: "imagenes"
        widget: "list"
        summary: '{{fields.foto}}'
        fields:
          - { label: "Imagen", name: "foto", widget: "image" }
          - { label: "Título / Pie de foto", name: "titulo_foto", widget: "string", required: false }